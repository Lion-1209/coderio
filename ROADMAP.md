# 路线图

[中文](ROADMAP.md) | [English](ROADMAP_en.md)

> 这份文档回答试用者最常见的问题：**这个项目还在维护吗、接下来做什么、明确不做什么。**
> 更新随版本发布进行；已完成的内容见 [CHANGELOG.md](CHANGELOG.md)。

## 维护状态

coderio 是单人业余项目，维护者用自有工作时间开发和维护。更新节奏不固定，
但持续进行：安全与正确性问题（P0/P1 级）优先于新功能修复；每个版本的可
见变化记录在 CHANGELOG。issue 会看，PR 欢迎——复杂改动建议先开 issue
对齐方向。

## Now — 正在做

修复优先于功能。当前的重心是让"可靠"这个承诺有真实证据支撑：

- **真实 provider 的 nightly eval**。现有的 1000+ 测试 mock 了模型层——
  它们证明不了真实 provider 的流式输出、tool-call 形态、限流行为的兼容
  性。计划建立至少覆盖智谱与阶跃两个 provider 的自动化真实验证，作为
  发版前的常规门。
- **固定的发版节奏**。修复与新功能按批次进入 CHANGELOG 的 Unreleased
  段，攒够一个版本就发布，不再让多个版本的改动积压在同一个版本号后面。
- **Windows 写沙箱的真隔离（ACL）**。当前的 job 档只做资源限制，write
  档与 job 等效——这是文档里如实标注的最大已知短板。计划用 Windows
  ACL 实现真正的文件写隔离。

## Next — 下一步

- **macOS 沙箱方案**：评估 `sandbox-exec` 落地；若成本过高，则明确宣布
  macOS 暂无 OS 级沙箱并保持文档口径（当前已是如此，但需要一个正式决策）。
- **ToolResult 结构化**：bash 的 exit_code 目前靠正则从结果字符串里提取
  （`[exit_code: N]` 标记），上游格式一变解析就断。计划改为结构化结果对象。
- **Textual 上游协作**：Screen layers 滚动渲染 bug 的最小复现提交给
  Textual 仓库（问题现象与复现测试在本仓已有）。
- **确认体验扩展**：文件写入已有 diff 预览；shell 命令确认计划增加风险
  标注（命令分类：读/写/网络/不可逆）。

## Later — 愿景

- **双定位强化**：coderio 的差异化始终是两条——"中文 Coding Plan 用户的
  本地 agent"（智谱/阶跃订阅直连，不需要转发层）和"能跑通、读得懂的
  agent 参考实现"（分层单体，每个安全层都可单独读）。路线图围绕这两条
  展开，不追逐其他 agent 的功能清单。
- **更广的 provider 生态**：在保持 Anthropic/OpenAI 双协议直连的前提下
  扩展国产 provider 支持。

## 非目标

明确不做的（减负也是承诺的一部分）：

- **多智能体编排框架**。曾经内置过 6-agent LangGraph 流水线，2026-07 已
  整体移除——单引擎 + 子代理（只读 research / 通用 task）已覆盖实际场景。
- **IDE 插件**。终端是 coderio 的形态。
- **云服务 / 遥测 / 账号体系**。本地工具，密钥留在本机，不收集任何数据。

## 反馈

安全与正确性问题请直接开 issue；使用问题附上 `coderio config` 的输出
（脱敏后）能加快定位。
